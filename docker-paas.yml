redis:
  image: redis:2.8
  restart: always

www:
  build: .
  ports:
    - "8080"
  links:
    - redis
  environment:
    - APP_PORT=8080
    - MONGO_URI
    - NODE_ENV=production
    - NPM_CONFIG_LOGLEVEL=info
    - SENTRY_DNS
  restart: always
